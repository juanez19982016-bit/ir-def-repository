{
  "name": "regex-named-groups",
  "version": "0.4.0",
  "description": "Make exec() provide named capture group results",
  "main": "lib",
  "keywords": [],
  "author": "Eli Doran <eli+npm@elidoran.com>",
  "license": "MIT",
  "scripts": {
    "compile": "coffee --bare --compile lib",
    "clean": "rm -rf lib/*.js",
    "prepublish": "coffee --bare --compile lib",
    "postpublish": "rm -rf lib/*.js",
    "test": "mocha --compilers coffee:coffee-script/register --reporter spec --bail --check-leaks test/lib"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/elidoran/node-regex-named-groups.git"
  },
  "bugs": "http://github.com/elidoran/node-regex-named-groups/issues",
  "devDependencies": {
    "coffee-script": "^1.10.0",
    "mocha": "^2.3.3",
    "through2": "^2.0.0"
  },
  "dependencies": {},
  "engines": {
    "node": ">=0.10"
  }
}
